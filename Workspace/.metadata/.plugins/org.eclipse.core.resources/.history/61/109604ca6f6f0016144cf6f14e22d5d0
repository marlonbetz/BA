import numpy as np
from scipy.stats import gaussian_kde

class ClusterWeighting(object):
    def __init__(self,distr):
        self.distr = distr
        self.kernel = gaussian_kde(distr)
        
    
    def getClusterLogLikelihood(self,cluster_members):
        return np.sum(np.array([np.log2(self.kernel.pdf(x)) for x in cluster_members]))

cl =  ClusterWeighting(np.random.normal(0,1,(100,2)))
print(cl.getClusterLogLikelihood(np.random.normal(0,1,(2,2))))