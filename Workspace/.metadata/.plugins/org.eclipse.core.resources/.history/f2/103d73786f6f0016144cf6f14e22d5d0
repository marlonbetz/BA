import numpy as np
from scipy.stats import gaussian_kde

class ClusterWeighting(object):
    def __init__(self,distr):
        self.distr = distr
        self.kernel = gaussian_kde(distr)
        
    
    def weightCluster(self,cluster_members):
        log_likelihood = np.sum(np.array([np.log2(self.kernel.pdf(x)) for x in cluster_members]))